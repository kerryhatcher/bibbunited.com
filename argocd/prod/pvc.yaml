apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: bibbunited-media
  namespace: civpulse-prod
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: local-path
  resources:
    requests:
      storage: 10Gi
