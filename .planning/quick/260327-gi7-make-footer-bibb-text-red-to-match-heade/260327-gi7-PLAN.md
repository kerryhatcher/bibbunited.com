---
phase: quick
plan: 260327-gi7
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/ui/Logo.tsx
autonomous: false
requirements: []
must_haves:
  truths:
    - "Footer logo BIBB text displays in red/accent color matching header style"
    - "Header logo remains unchanged"
  artifacts:
    - path: "src/components/ui/Logo.tsx"
      provides: "Logo component with accent-colored BIBB text in footer variant"
  key_links: []
---

<objective>
Make the footer Logo's BIBB text red (accent color) to match the header logo color scheme.

Purpose: Visual consistency between header and footer branding.
Output: Updated Logo.tsx with accent-colored BIBB text for footer variant.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@src/components/ui/Logo.tsx
@src/components/layout/Footer.tsx

Current state: The Logo component has a `variant` prop. In the footer variant, BIBB inherits white from the footer background context via `logo-bibb` CSS class (`color: inherit`), while UNITED is explicitly `text-white`. In the header (default variant), BIBB inherits dark text and UNITED is `text-accent` (crimson red).

Goal: Footer variant should show BIBB in `text-accent` (red) instead of inheriting white.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add accent color to BIBB text in footer Logo variant</name>
  <files>src/components/ui/Logo.tsx</files>
  <action>
In the Logo component, add a `bibbColor` variable similar to the existing `unitedColor`:
- When `variant === 'footer'`: set BIBB span to `text-accent` (red)
- When `variant === 'default'`: keep BIBB span using `logo-bibb` class (inherits dark text from context)

Apply the computed class to the BIBB span. Keep the `logo-bibb` class on the default variant. The footer variant replaces `logo-bibb` with `text-accent`.

Do NOT change the UNITED text behavior -- it stays `text-white` for footer and `text-accent` for default.
  </action>
  <verify>
    <automated>cd /home/kwhatcher/projects/civicpulse/bibbunited.com && grep -n "text-accent" src/components/ui/Logo.tsx | wc -l</automated>
    Expect 2 occurrences of `text-accent` in Logo.tsx (one for BIBB footer, one for UNITED default).
  </verify>
  <done>Footer Logo BIBB text uses text-accent (red). Header Logo BIBB text unchanged (logo-bibb / inherit). UNITED text behavior unchanged for both variants.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Footer BIBB text now displays in red/accent color matching header branding</what-built>
  <how-to-verify>
    1. Start the dev server: `docker compose up`
    2. Visit http://localhost:3000
    3. Scroll to the footer
    4. Verify BIBB text in the footer logo is red (accent/crimson color)
    5. Verify UNITED text in the footer remains white
    6. Scroll back to top and verify header logo is unchanged (dark BIBB, red UNITED)
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
- Footer logo BIBB text renders in accent red color
- Header logo BIBB text remains dark (inherited)
- No visual regression in either header or footer
</verification>

<success_criteria>
Footer BIBB text matches header's red accent branding. Both logo variants render correctly in their respective contexts.
</success_criteria>

<output>
After completion, create `.planning/quick/260327-gi7-make-footer-bibb-text-red-to-match-heade/260327-gi7-SUMMARY.md`
</output>
