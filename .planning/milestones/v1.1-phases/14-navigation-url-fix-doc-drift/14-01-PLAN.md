---
phase: 14-navigation-url-fix-doc-drift
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/seed.ts
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
autonomous: true
requirements:
  - VIS-02
  - UX-02
gap_closure: true

must_haves:
  truths:
    - "Clicking Contact Officials in navigation loads the Contact Officials page (no 404)"
    - "Active nav indicator and aria-current fire correctly on the Contact Officials page"
    - "All implemented v1.1 requirement checkboxes are marked [x] in REQUIREMENTS.md"
    - "All completed requirement traceability entries show Complete status"
  artifacts:
    - path: "src/seed.ts"
      provides: "Correct navigation URL for Contact Officials"
      contains: "url: '/contact-officials'"
    - path: ".planning/REQUIREMENTS.md"
      provides: "Accurate requirement completion status"
      contains: "[x] **VIS-02**"
    - path: ".planning/ROADMAP.md"
      provides: "Accurate phase completion status"
      contains: "Phase 14"
  key_links:
    - from: "src/seed.ts"
      to: "Navigation global in database"
      via: "pnpm seed re-run"
      pattern: "url: '/contact-officials'"
    - from: "Navigation global"
      to: "src/components/layout/Header.tsx isActiveLink()"
      via: "pathname === href comparison"
      pattern: "pathname === href"
---

<objective>
Fix the Contact Officials navigation URL mismatch in seed data and correct all documentation drift in REQUIREMENTS.md and ROADMAP.md.

Purpose: Close the two remaining v1.1 gaps (VIS-02, UX-02) so the milestone is fully complete.
Output: Fixed seed.ts, re-seeded database, updated REQUIREMENTS.md and ROADMAP.md with accurate completion status.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/14-navigation-url-fix-doc-drift/14-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix Contact Officials seed URL and re-seed database</name>
  <files>src/seed.ts</files>
  <read_first>
    - src/seed.ts (line 469 — the URL to fix)
    - src/app/(frontend)/contact-officials/page.tsx (confirms the actual route path)
    - src/components/layout/Header.tsx (confirms isActiveLink logic is correct — DO NOT modify this file)
    - docker-compose.dev.yml (to determine how to run seed in docker environment)
    - package.json (to confirm seed script name)
  </read_first>
  <action>
1. In src/seed.ts, change line 469 from:
   `{ label: 'Contact Officials', type: 'external', url: '/officials' },`
   to:
   `{ label: 'Contact Officials', type: 'external', url: '/contact-officials' },`

2. Re-seed the database. The dev environment runs via docker compose. Run:
   `docker compose -f docker-compose.dev.yml exec app pnpm seed`
   If the service name is different, check docker-compose.dev.yml for the correct service name. If the app service is not running, start it first with `docker compose -f docker-compose.dev.yml up -d` then run the seed command.

3. DO NOT modify Header.tsx — the isActiveLink() function is correct. The bug is data-only.
4. DO NOT add route aliases or redirects from /officials to /contact-officials.

Per VIS-02: This fix ensures the Contact Officials nav link points to the correct route.
Per UX-02: This fix enables isActiveLink() to match pathname === href on the Contact Officials page, activating the active nav indicator and aria-current attribute.
  </action>
  <verify>
    <automated>grep -n "url: '/contact-officials'" src/seed.ts | grep -q "Contact Officials" && echo "PASS: seed URL fixed" || echo "FAIL: seed URL not fixed"</automated>
  </verify>
  <acceptance_criteria>
    - src/seed.ts line 469 contains `url: '/contact-officials'` (not `url: '/officials'`)
    - src/seed.ts does NOT contain the string `url: '/officials'` on any navigation item line
    - No changes made to src/components/layout/Header.tsx
  </acceptance_criteria>
  <done>seed.ts has correct Contact Officials URL pointing to /contact-officials, database re-seeded with updated navigation data</done>
</task>

<task type="auto">
  <name>Task 2: Fix documentation drift in REQUIREMENTS.md and ROADMAP.md</name>
  <files>.planning/REQUIREMENTS.md, .planning/ROADMAP.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (current checkbox and traceability state)
    - .planning/ROADMAP.md (current plan checkbox and phase status)
    - .planning/phases/14-navigation-url-fix-doc-drift/14-RESEARCH.md (documentation drift inventory in lines 98-125)
  </read_first>
  <action>
1. In .planning/REQUIREMENTS.md, update these checkboxes from `[ ]` to `[x]`:
   - Line with `**VIS-02**`: change `- [ ] **VIS-02**` to `- [x] **VIS-02**`
   - Line with `**UX-02**`: change `- [ ] **UX-02**` to `- [x] **UX-02**`

   NOTE: The research identified 6 additional checkboxes (A11Y-01, A11Y-02, A11Y-04, UX-01, UX-03, UX-04) as needing update, but reading the current REQUIREMENTS.md these are ALREADY marked [x]. Verify at execution time — only update items that are actually still `[ ]`.

2. In .planning/REQUIREMENTS.md traceability table, update these rows:
   - VIS-02: change `Pending` to `Complete`
   - UX-02: change `Pending` to `Complete`

   Again verify the other 6 traceability items — if any still show "Pending" for completed work, update those too.

3. In .planning/ROADMAP.md:
   - Verify Phase 9 and Phase 12 plan checkboxes are `[x]` (research notes these may already be fixed)
   - Update Phase 14 plan checkbox: change `- [ ] 14-01-PLAN.md` to reflect the plan objective
   - Update Phase 14 progress row: status should remain "Planning" (execute-phase will mark complete)

4. Update the REQUIREMENTS.md coverage/last-updated line at the bottom to reflect today's date: `*Last updated: 2026-03-25 after Phase 14 gap closure*`
  </action>
  <verify>
    <automated>grep "\[x\] \*\*VIS-02\*\*" .planning/REQUIREMENTS.md && grep "\[x\] \*\*UX-02\*\*" .planning/REQUIREMENTS.md && grep "VIS-02.*Complete" .planning/REQUIREMENTS.md && grep "UX-02.*Complete" .planning/REQUIREMENTS.md && echo "PASS: all doc updates applied" || echo "FAIL: documentation drift remains"</automated>
  </verify>
  <acceptance_criteria>
    - .planning/REQUIREMENTS.md contains `[x] **VIS-02**`
    - .planning/REQUIREMENTS.md contains `[x] **UX-02**`
    - .planning/REQUIREMENTS.md traceability table has `VIS-02 | Phase 14 | Complete`
    - .planning/REQUIREMENTS.md traceability table has `UX-02 | Phase 14 | Complete`
    - .planning/REQUIREMENTS.md has zero `[ ]` checkboxes in the v1.1 section (all 26 requirements marked [x])
    - .planning/REQUIREMENTS.md has zero `Pending` entries in the traceability table
    - .planning/ROADMAP.md Phase 9 and Phase 12 plan items all show `[x]`
  </acceptance_criteria>
  <done>All v1.1 requirement checkboxes marked complete, all traceability entries show Complete, ROADMAP reflects accurate plan completion status</done>
</task>

</tasks>

<verification>
1. `grep "url: '/contact-officials'" src/seed.ts` returns the Contact Officials nav item
2. `grep -c "\[ \]" .planning/REQUIREMENTS.md` returns 0 for v1.1 section (all checked)
3. `grep -c "Pending" .planning/REQUIREMENTS.md` returns 0 in traceability table
4. No modifications to src/components/layout/Header.tsx
</verification>

<success_criteria>
- Contact Officials navigation URL in seed.ts points to /contact-officials
- Database re-seeded with corrected navigation data
- All 26 v1.1 requirements marked [x] in REQUIREMENTS.md
- All traceability entries show Complete status
- ROADMAP.md accurately reflects all plan completion states
</success_criteria>

<output>
After completion, create `.planning/phases/14-navigation-url-fix-doc-drift/14-01-SUMMARY.md`
</output>
