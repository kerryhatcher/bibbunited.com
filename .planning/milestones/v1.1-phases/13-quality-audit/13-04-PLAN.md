---
phase: 13-quality-audit
plan: 04
type: execute
wave: 2
depends_on: [13-03]
files_modified:
  - AUDIT-RESULTS.md
  - e2e/audit/results/
autonomous: false

requirements: [QA-01]
gap_closure: true

must_haves:
  truths:
    - "All 30 axe-core accessibility tests pass with zero violations"
    - "Lighthouse scores meet 95+ threshold on all 6 routes at all 5 viewports"
    - "AUDIT-RESULTS.md reflects updated passing scores"
  artifacts:
    - path: "AUDIT-RESULTS.md"
      provides: "Updated audit results showing passing scores after gap fixes"
      contains: "PASS"
    - path: "e2e/audit/results/"
      provides: "Updated JSON results from re-run"
  key_links:
    - from: "AUDIT-RESULTS.md"
      to: "e2e/audit/results/*.json"
      via: "Generated from re-run test data"
      pattern: "PASS"
---

<objective>
Re-run the full audit suite (Lighthouse + axe-core + admin login) to verify gap closure fixes, then update AUDIT-RESULTS.md with new passing results.

Purpose: Validates that the 3 code fixes from plan 13-03 resolve the verification failures. Produces the final audit report.
Output: Updated AUDIT-RESULTS.md with passing scores, updated JSON result files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-quality-audit/13-03-SUMMARY.md
@.planning/phases/13-quality-audit/13-VERIFICATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Re-run audit suite and update results</name>
  <files>AUDIT-RESULTS.md, e2e/audit/results/</files>
  <read_first>
    - AUDIT-RESULTS.md (current audit results to understand format and structure)
    - e2e/audit/accessibility.spec.ts (axe-core test spec to understand what runs)
    - e2e/audit/lighthouse.spec.ts (lighthouse test spec to understand what runs)
    - e2e/audit/admin-login.spec.ts (admin test spec)
    - e2e/helpers/audit-helpers.ts (helper functions for result generation)
    - .planning/phases/13-quality-audit/13-03-SUMMARY.md (verify gap fixes were applied)
  </read_first>
  <action>
    1. Ensure dev server is running via docker compose (check `curl -s -o /dev/null -w "%{http_code}" http://localhost:3000` returns 200, start if needed with `docker compose -f docker-compose.dev.yml up -d`)

    2. Clear old audit results: `rm -f e2e/audit/results/*.json`

    3. Run the axe-core accessibility tests first (these were the most broken — all 30 failed):
       `npx playwright test e2e/audit/accessibility.spec.ts --reporter=list`
       Expected: All 30 tests pass (zero violations now that footer contrast is fixed)

    4. Run the Lighthouse audit tests:
       `npx playwright test e2e/audit/lighthouse.spec.ts --reporter=list --workers=1`
       Expected: All 30 route-viewport combinations score 95+ (news article no longer 500s, about page has meta description)

    5. Run the admin login test:
       `npx playwright test e2e/audit/admin-login.spec.ts --reporter=list`
       Expected: Pass

    6. Update AUDIT-RESULTS.md with new results:
       - Update the date to today (2026-03-25)
       - Update all score tables with actual scores from the JSON results in `e2e/audit/results/`
       - Change FAIL entries to PASS where applicable
       - Update the summary section to reflect all tests passing
       - Update the Future Polish section — remove items that were fixed (footer contrast, news 500, about SEO) and keep any remaining polish items
       - Add a "Gap Closure" section noting the 3 fixes applied and re-verification results

    7. Run the existing 160 regression tests to confirm no regressions from the fixes:
       `npx playwright test e2e/responsive/ --reporter=list`
  </action>
  <verify>
    <automated>grep -c "PASS" AUDIT-RESULTS.md</automated>
  </verify>
  <acceptance_criteria>
    - AUDIT-RESULTS.md contains updated date (2026-03-25)
    - AUDIT-RESULTS.md accessibility section shows 0 violations for all 30 route-viewport combinations
    - AUDIT-RESULTS.md lighthouse section shows 95+ scores for all routes including /news/[slug] and /about
    - e2e/audit/results/ contains fresh JSON files from the re-run
    - No FAIL entries remain in the main score tables (Future Polish section may still note aspirational items)
  </acceptance_criteria>
  <done>All audit tests pass after gap fixes. AUDIT-RESULTS.md updated with passing scores. Phase 13 quality audit is complete.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify audit results and approve phase completion</name>
  <files>AUDIT-RESULTS.md</files>
  <action>
    Human reviews the updated AUDIT-RESULTS.md and visually verifies the footer fixes in the browser.
    This is a blocking checkpoint — Phase 13 cannot be marked complete without user approval.
  </action>
  <what-built>
    Fixed 3 verification gaps and re-ran the complete audit suite:
    1. Footer CTA and Logo contrast fixed (white on navy = 11.07:1 ratio)
    2. News article null-safety crash fixed (author?.displayName fallback)
    3. About page meta description fallback added
    All 30 axe-core tests and 30 Lighthouse tests should now pass.
  </what-built>
  <how-to-verify>
    1. Open AUDIT-RESULTS.md and review the updated scores
    2. Confirm all routes show PASS status
    3. Confirm Lighthouse scores are 95+ across all categories
    4. Confirm axe-core shows 0 violations on all routes
    5. Visit http://localhost:3000/news/school-board-approves-2026-2027-budget — confirm it loads without error
    6. Scroll to footer — confirm CTA button text is white (readable) and Logo UNITED text is white
  </how-to-verify>
  <verify>User confirms approval</verify>
  <done>User has reviewed and approved the final audit results. Phase 13 is complete.</done>
  <resume-signal>Type "approved" to complete Phase 13, or describe any issues</resume-signal>
</task>

</tasks>

<verification>
- All 30 axe-core tests pass (zero WCAG violations)
- All 30 Lighthouse tests pass (95+ on all categories)
- Admin login test passes
- AUDIT-RESULTS.md reflects passing results
- No new regressions in existing 160 e2e tests
</verification>

<success_criteria>
Phase 13 Quality Audit is fully verified: all automated checks pass, all verification gaps are closed, and the final audit report documents production-ready quality.
</success_criteria>

<output>
After completion, create `.planning/phases/13-quality-audit/13-04-SUMMARY.md`
</output>
